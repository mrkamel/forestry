---
label: teaser
hide_body: true
fields:
- name: title
  type: text
  config:
    required: false
  label: title
- name: background_color
  type: color
  label: background color
  config:
    required: false
    color_format: Hex
- name: icon
  type: file
  config:
    maxSize: 64
  label: icon
- name: image
  type: file
  config:
    maxSize: 64
  label: image
- name: description
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: description
- name: buttonlink
  type: text
  config:
    required: false
  label: buttonLink
- name: link
  type: text
  config:
    required: false
  label: link
pages:
- homepage/teasers/inside-business-das-b2b-magazin.md
